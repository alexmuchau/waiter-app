version: '3.8'
services:
  mysql:
    image: db-waiter:1.1
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: db
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    volumes:
      - db-volume:/var/lib/mysql
    ports:
      - 3306:3306
      
volumes:
    db-volume: